#!/usr/bin/env sh
set -e

echo "🚀 Running pre-push checks..."

# Type checking (will exit on failure)
echo "📝 Type checking..."
npm run typecheck

# Build test (will exit on failure)
echo "🏗️  Testing build..."
npm run build

echo "✅ Pre-push checks passed!"
